/**
 * Init orchestration core.
 *
 * Pure orchestration with injected dependencies.
 * CLI adapter provides process-bound implementations.
 */

// Placeholder — init orchestration added in Task 2.
// Accepts deps: exists(path), install(packages, cwd), copy(file), log/warn.
// Returns structured events for adapter printing.
